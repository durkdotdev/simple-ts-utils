---
title: "Requester"
description: "Requester is a simple asynchronous function to easily create HTTP API requests."
---

import SEO from "../../../components/SEO";

<SEO
  title="Requester"
  description="Requester is a simple asynchronous function to easily create HTTP API requests."
  url="simple-ts-utils.durk.dev/utils/requester"
/>

# Requester

**Requester** is a simple asynchronous function to easily create HTTP API requests.

## Params

- `url` - a string that represents the endpoint to fetch data from
- `method` (optional, default = `GET`) - a string with the value `GET`, `DELETE`, `POST`, or `PUT`
- `body` (optional) - an object containing JSON data to be sent in the request's body

## Usage

### GET

```jsx
import { requester } from "simple-ts-utils";

const data = await requester("/api/posts");

// data = await data.json();
// { "posts": [...] }
```

### POST With Data

```jsx
import { requester } from "simple-ts-utils";

const data = await requester("/api/posts", "POST", {
  content: "Content",
  title: "Title"
});

// data = await data.json();
// { "post": { "id": "1234" }}
```
